# 計畫:エリアワゴンを検索エンジンに統合(PoC = 穂波・菰田地区)

> 目的:今日の議論で確認した「本作品の唯一の代替不可能な価値=Google が知らない在地交通(乗合タクシー・エリアワゴン)の統合」を、文字説明から**実際に検索できる形**へ引き上げる。まず 1 地区(穂波・菰田)で PoC を作り、効果を見てから他地区へ拡張するか判断する。
>
> 作成:2026-07-13。

---

## ✅ 進捗(2026-07-14 時点):穂波・菰田 PoC 完了

**この地区の統合は完了・検証済み。** 次の作業者向けの現況:

- P1〜P6 すべて完了。`node scripts/test_engine.mjs` 全PASS(ワゴンケース含む)、Chrome実機確認済み。
- キラーデモ成立:**飯塚記念病院**(コミュニティバス徒歩圏外)→ エリアワゴンで「飯塚記念病院入口」まで100円で到達、と検索結果に出る。
- 実装ファイル:
  - `data/wagon/honami.json` … 時刻表(15便)の中間データ(手起こし)
  - `data/wagon/honami.geocoded.json` … 停留所座標(`scripts/geocode_wagon.py honami` で再生成可)
  - `scripts/build_app_data.py` の `WAGON_FEEDS` / `load_wagon()` … ここに地区を足せば拡張
  - `app/app.js` … `directRides` は walkOf で最寄り停留所降車、`fareFor` は `fares.flat`、`routeBadge` でワゴン橙色
- **既知の限界**:座標14停は隣接内挿で精度低め(主要施設は実測)。ワゴン↔バスの乗換は未対応(意図的)。予約乗合タクシー(300円DRT)は時刻表化せず案内のみ。

### 他地区へ拡張する手順(1地区あたり ~3h)
1. `data/wagon_pdf/` の該当PDFを Read で時刻表抽出 → `data/wagon/<地区>.json` を honami.json と同じ形式で作成
2. `python scripts/geocode_wagon.py <地区>` で座標解決 → 不足分は `stopsOverride` に手入力
3. `scripts/build_app_data.py` の `WAGON_FEEDS` に `<地区>` を追加
4. `python scripts/build_app_data.py` → `node scripts/test_engine.mjs` → Chrome確認

**以下は当初計画(記録として保持)。**

---

## 0. なぜ穂波・菰田地区から始めるか

- PDF(`data/wagon_pdf/honami_komota_13433.pdf`)の**時刻表がテキスト抽出可能**(スキャン画像ではない)。手打ちゼロではないが半自動化できる。← 最大の追い風
- 停留所に **「市立病院」「飯塚記念病院入口」** が含まれる。飯塚記念病院は**コミュニティバス検索では「近くにバス停なし」だった病院**。
  → **キラーデモ:「バスでは行けない病院に、エリアワゴンなら 100 円で行ける。Google はそれを知らない」** が 1 画面で成立。
- 菰田は殺手鐧疊圖の重災区(バス徒歩圏消滅・高齢者多数)。分析ページの主張と検索機能が直結する。

---

## 1. 対象データ(PDF から確認済みの構造)

`honami_komota_13433.pdf` には 2 系統ある:

| 系統 | 運行日 | 便数 | 備考 |
|---|---|---|---|
| 穂波・菰田地区(本系統) | 月・火・金・土 / 一部 月〜土 | 9便 | 1〜6便=月火金土、7〜9便=月〜土 |
| 穂波地区(高田系統) | 水・木・土 | 6便 | 水木=1〜4便、土=1〜6便。1便/5便・3便/6便は停留所列を共用(時刻列が別) |

- 運賃:**エリアワゴン 100 円均一**(介護者無料)。回数券共通。
- 「▼」記号=**通過**(その停留所には停まらない)。解析時に除外が必要。
- 各便は「停留所→時刻」の明確な順序列。方向性は考えず PDF の順序をそのまま使う。

### 停留所一覧(約 40、重複除く)
南尾 / 野間公園 / 神の浦浦田 / 忠隈浦田 / 忠隈住民センター / 五穀神 / 菰田市営住宅 / 菰田交流センター / 飯塚駅前 / 若菜小学校 / 小正浦の原 / 市立病院 / 椿ポンプ場 / ナフコ飯塚南店 / ハローデイ穂波店 / 穂波交流センター / 穂波支所 / ゆめタウン飯塚 / トライアル飯塚店 / 徳前 / イオン穂波店 / 穂波福祉総合センター / カホテラス / 鶴三緒 / 飯塚記念病院入口 / 西運送車庫 / 高田炭焼 / 天寿園 / 高田公民館 / 青山医院 / 津原老松神社 / 津原公民館 / 津原橋 / 安恒公民館 / 椋本前田 / 椋本公民館 / コメリ飯塚店 / 天道駅 / 西鉄楽市公園 / 巻き上げ機台座(新設) / セブンイレブン飯塚平恒店 / 倉光食品前

### 座標(geocode)の見込み
- **既存データから流用可能**(筑穂・高田線 GTFS / BODIK 施設):市立病院・コメリ飯塚店・イオン穂波店・穂波福祉総合センター・徳前・トライアル飯塚店・ゆめタウン飯塚・津原橋・ナフコ飯塚南店 など約 10〜15
- **要 geocode**(公民館・神社・住民センター・店舗):残り約 25〜30。地名検索(Nominatim 等)+ PDF 路線図で目視補正。多くは著名地物なので現実的。

---

## 2. 設計方針

### 2-1. 中間データ形式(人手で PDF から起こす)
GTFS ではないので、手入力しやすい中間 JSON を新設し、ビルド時に既存 GTFS feed と**同一構造**へ変換する。

`data/wagon/honami.json`(案):
```jsonc
{
  "feed": "wagon_honami",
  "routeName": "エリアワゴン(穂波・菰田)",
  "fareFlat": 100,
  "services": {
    "月火金土": { "days": [1,1,0,0,1,1,0] },   // days = [月,火,水,木,金,土,日]
    "水木土":   { "days": [0,0,1,1,0,1,0] },
    "月〜土":   { "days": [1,1,1,1,1,1,0] }
  },
  "stops": {                                   // 停留所名 -> [lat, lon]
    "市立病院": [33.xxxx, 130.xxxx]
  },
  "trips": [
    { "service": "月火金土", "name": "1便",
      "stopTimes": [ ["南尾","8:10"], ["野間公園","8:11"], ["市立病院","8:39"] ] }
  ]
}
```
- `▼`(通過)は起こす段階で除外する。
- 停留所名は GTFS stops と衝突しないよう `wagon_honami:` で名前空間化(既存の feed 方式と同じ)。

### 2-2. 検索エンジンへの統合(app.js はほぼ無改修で済む想定)
現行エンジンは既に **複数 feed + service カレンダー + feed 別運賃** に対応済み:
- `D.stops` / `D.trips`(`feed`,`service`,`st`)/ `D.services[feed][serviceId]` に **wagon_honami を 3 本目の feed として追加**するだけ。
- `serviceActive()` は `days[]` を見るので「月火金土」等はそのまま動く(GTFS の calendar と同形式)。
- 運賃:`fares` に `wagon_honami_flat: 100` を追加、`fareFor()` に分岐 1 行。

### 2-3. UI での区別
- ルート名バッジの色をバス(緑)と変える(例:オレンジ=ワゴン)。
- 運賃 100 円を強調(バス 170〜200 円より安い)。
- 結果カードに「🚐 エリアワゴン」表記。予約不要である旨を明記(予約乗合タクシーと混同させない)。

### 2-4. 予約乗合タクシー(300 円・真の予約制 DRT)は別扱い
時刻表化しない。`data/wagon_pdf/yoyaku_taxi_13426.pdf` を確認し、**予約電話・締切・対象範囲**を wagon カードに追記(+ 可能なら `tel:` ワンタップ発信)。今回の PoC では優先度低。

---

## 3. 作業ステップ

| # | ステップ | 内容 | 見積 |
|---|---|---|---|
| P1 | 中間形式 + ビルド骨格 | `data/wagon/honami.json` スキーマ確定、`build_app_data.py` に wagon ローダー追加(まだ空データで通す) | 0.5h |
| P2 | 時刻表を起こす | PDF テキストから 9+6 便の stopTimes を `honami.json` へ。`▼` 除外。**人手校正必須** | 1.5h |
| P3 | 停留所 geocode | 既存流用 → 残りを geocode/目視。`honami.json` の `stops` を埋める | 1.5h |
| P4 | エンジン統合 | wagon を 3 本目 feed として `data.js` 生成。運賃分岐・曜日・UI バッジ | 1h |
| P5 | テスト | `test_engine.mjs` に wagon ケース追加(飯塚記念病院入口へ到達、水木は高田系統のみ 等)。Chrome 実機確認 | 1h |
| P6 | ナラティブ更新 | 結果画面 wagon カード刷新(文字→検索可能)、analysis に「バス空白×ワゴン補完」を追記 | 0.5h |

合計 **約 6 時間相当**(1 地区)。成功後、他地区は P2+P3 の繰り返し(1 地区あたり 3h 目安)。

---

## 4. 受け入れ基準(この PoC が「できた」と言える条件)

1. **飯塚記念病院**を行き先に選ぶと、従来の「近くにバス停なし」ではなく、**エリアワゴンで「飯塚記念病院入口」まで行ける便(時刻・100 円)**が出る。
2. 郊外の停留所(例:天寿園)→ 市立病院 で、エリアワゴンの時刻・運賃が出る。
3. 曜日カレンダーが正しい(水曜は本系統が動かず高田系統のみ、日曜は運休、等)。
4. バス停一覧の 4 状態色分けが wagon 停留所にも効く。
5. `node scripts/test_engine.mjs` 全 PASS(wagon ケース含む)。
6. Chrome 実機で 3 ステップが破綻なく通る。

---

## 5. リスク・未確定点(正直な列挙)

- **geocode 精度**:公民館・神社など小地名の座標がずれると徒歩距離判定が狂う。→ 主要病院・施設を優先し、ずれても実害が小さい停留所は許容。
- **時刻表の起こしミス**:停留所列と時刻列の対応ずれ。→ P2 で人手校正、P5 でテスト。数字は出典 PDF と目視突合。
- **エリアワゴン↔バスの乗換**:今回は組まない(ワゴン内直通 + 徒歩のみ)。乗換まで欲張ると破綻リスク。将来課題。
- **「路線ワゴン」との区別**:一部地区は路線ワゴン。穂波・菰田は今回エリアワゴンのみ対象。
- **出典の鮮度**:PDF は現行版だが改正され得る。フッターに出典・確認日を明記し「最新は市ページで」を維持。
- **拡張の是非**:1 地区で工数実測してから 10 地区展開を判断。全部やる前提にしない(silent scope creep を避ける)。

---

## 6. この PoC が作品全体に与える意味

- 「Google は徒歩 3 時間か自家用車しか出さない。エリアワゴンという 100 円の答えを知らない」という主張が、**スライドの文字ではなく動く検索**として証明される。
- 分析ページ(課題)と検索アプリ(解決)が、**同じ停留所データ**で一本につながる。
- 開放データ(PDF のみ・機械可読でない)を**自力で機械可読化して検索に載せた**という、コンテストの主旨(オープンデータで地域課題を解決)への直球の回答になる。
